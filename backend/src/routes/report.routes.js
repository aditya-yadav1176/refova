'use strict';

// report routes are embedded in referral.routes.js (/api/referrals/:id/report)
// This is a placeholder in case standalone report routes are needed later.
const { Router } = require('express');
const router = Router();
module.exports = router;
